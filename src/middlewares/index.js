const regla_campo = require("./reglas_campos");
 const reglas_producto= require("./reglas_producto");
const { validarCampos } = require("./validar-campos");

module.exports = {
  regla_campo,
  validarCampos,
  reglas_producto
};
